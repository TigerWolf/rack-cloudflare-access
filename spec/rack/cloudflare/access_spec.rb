# frozen_string_literal: true

RSpec.describe Rack::Cloudflare::Access do
  it "has a version number" do
    expect(Rack::Cloudflare::Access::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
